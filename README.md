# 假期安排 uTools 插件

用于按大小休周期计算月度休息日，并从官方放假安排中筛出真正增加的法定假期。

- 日历可切换每周从星期几开始，默认周日。
- 日历使用不同颜色的下边框区分大周和小周，不显示周型文字标签。

## 计算口径

- `法定假`：从连续放假区间中先去除当前大小休配置的休息日，再按法定天数保留靠前的工作日。
- `休息日重合`：完全按照大小休配置显示普通休息，不额外标记节日或重合状态。
- `调休`：工作日借调休形成的休息，只显示调休，不计入法定假。
- `法定调休`：覆盖大小休规则，最终按上班处理。

内置数据为 2026 年国务院办公厅公布的放假调休安排；法定日期按现行《全国年节及纪念日放假办法》拆分。进入本地尚无数据的年份时，插件会从 `NateScarlet/holiday-cn` 自动获取并保存；获取失败不会覆盖已有数据。其他年份也可在日历中逐日维护，或通过 JSON 导入。

## 本地运行

浏览器预览时，在当前目录启动任意静态服务器：

```bash
python3 -m http.server 4173
```

## uTools 载入

1. 打开 uTools 开发者工具。
2. 新建项目并选择本目录的 `plugin.json`。
3. 进入“假期安排”功能。

设置与节假日数据优先保存在 `utools.dbStorage`，普通浏览器预览时回退到 `localStorage`。首次获取新年份数据时需要联网。

## 目录结构

```text
assets/            插件图标与界面图片
src/app.js         页面交互与状态管理
src/styles.css     页面样式
src/holiday/       假期计算、内置数据和远端获取
tests/             业务逻辑测试
index.html         uTools 页面入口
plugin.json        uTools 插件配置
```

## 测试

```bash
npm test
```
